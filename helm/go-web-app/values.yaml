replicaCount: 1

image:
  repository: saas-go-web-app
  tag: "21199948482"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: go-web-app.local
      paths:
        - path: /
          pathType: Prefix
